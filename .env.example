# Database Configuration
POSTGRES_USER=lazer_user
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=lazer

# Application
NODE_ENV=development
PORT=5000

# Database URL (será construída automaticamente pelo docker-compose)
# DATABASE_URL=postgresql://lazer_user:your_secure_password_here@localhost:5432/lazer?schema=public

# IMPORTANTE: 
# 1. Copie este arquivo para .env
# 2. Altere TODAS as senhas para valores seguros
# 3. NUNCA commite o arquivo .env no Git
# 4. Use senhas fortes com pelo menos 16 caracteres, incluindo letras, números e símbolos
